<!DOCTYPE html>
<html lang="pt-BR">
<%- include('../../partials/head') %>

  <body class="bg-light">

    <%- include('../../partials/header') %>

      <div class="d-flex">
        <%- include('../../partials/sidebar') %>

          <div class="content">
            <h1>Listar</h1>
            <table class="table">
              <thead>
                <tr>
                  <th>#</th>
                  <th>Nome</th>
                  <th>Ações</th>
                </tr>
              </thead>
              <tbody>
                <% categorias.forEach((categoria, i)=> { %>
                  <tr>
                    <td>
                      <%= i+1 %>
                    </td>
                    <td>
                      <%= categoria.nome %>
                    </td>
                    <td class="d-flex gap-2">
                      <form action="/categoria/editar/<%=categoria.id%>" method="get">
                        <button type="submit" class="btn btn-primary">Editar</button>
                      </form>
                      <form class="form-excluir" action="/categoria/excluir/<%=categoria.id%>" method="post">
                        <button type="submit" class="btn btn-danger">Excluir</button>
                      </form>
                    </td>
                  </tr>
                  <% }) %>
              </tbody>
            </table>
          </div>
      </div>

      <%- include('../../partials/scripts') %>
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        <script>
          const formsExcluir = document.getElementsByClassName('form-excluir');
          for (const formExcluir of formsExcluir) {
            formExcluir.addEventListener('submit', (e) => {
              e.preventDefault();
              Swal.fire({
                backdrop: false,
                title: 'Deseja realmente excluir?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'Sim',
                cancelButtonText: 'Cancelar'
              }).then((result) => {
                if (result.isConfirmed) {
                  formExcluir.submit();
                }
              });
            });
          }
        </script>


        <% if (mensagem) { %>

          <script>
            Swal.fire({
              backdrop: false,
              icon: '<%= mensagem.tipo %>',
              text: '<%= mensagem.valor %>'
            });
          </script>

          <% } %>
  </body>

</html>